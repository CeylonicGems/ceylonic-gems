import{requireRole}from"@/lib/auth";import{SellerSubmitForm}from"@/components/seller-submit-form";export default async function SubmitGem(){const{profile}=await requireRole(["seller","both"]);if(profile.verification_status!=="verified")return <section className="section top-section narrow"><div className="panel"><h1>Verification required</h1><p>Complete identity verification before submitting gemstones.</p><a className="button primary" href="/verification">Complete Verification</a></div></section>;return <section className="section top-section narrow-wide"><SellerSubmitForm/></section>}
