import{AppointmentForm}from"@/components/appointment-form";import{getPublishedGems}from"@/lib/data";export default async function AppointmentPage(){const gems=await getPublishedGems();return <section className="section top-section split"><div><span className="eyebrow">PRIVATE VIEWING</span><h1>Book an appointment.</h1><p>Request a physical viewing, video inspection, gemologist consultation, certificate review or international purchase discussion.</p><div className="notice">Exact seller locations remain hidden until an appointment is approved.</div></div><AppointmentForm gems={gems.map(({id,name})=>({id,name}))}/></section>}
